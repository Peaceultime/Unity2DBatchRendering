public class HexSystem
{
    /*public float time;

    private EndSimulationEntityCommandBufferSystem commandBufferSystem;
    protected override void OnCreate()
    {
        commandBufferSystem = World.GetOrCreateSystem<EndSimulationEntityCommandBufferSystem>();
        query = GetEntityQuery(ComponentType.ReadOnly<Hex>());

        time = 0;
    }
    protected override void OnUpdate()
    {
        time += Time.DeltaTime;
        //NativeArray<Entity> hexes = query.ToEntityArray(Allocator.TempJob);
        //EntityCommandBuffer commandBuffer = commandBufferSystem.CreateCommandBuffer();

        //hexes.Dispose();
    }*/
}